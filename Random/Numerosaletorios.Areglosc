// Programa que asigne valores a un vector con números aleatorios entre 12 y 35 incluidos
#include <stdio.h>
#include<stdlib.h>
#include<time.h>
int main(){

int vector[20];
srand(time(NULL));
for(int i=0; i<20; i++){
    vector[i]= 12+rand()%(32-12+1); //v[i]=rand()%10; nos daria numeros aleatorios entre 0-9
    //variable= limite_inferior+rand()% (limite_superior +1-limite inferior)
    printf("%d ", vector[i]);
}


    return 0;
}